// const FOLDER_ID = "1-eHh0OqpukHsHbsLoLbHeTEawYCzgRxH"; // FOLDER DIAS PRUEBA

// const CONCENTRADO_ID = "1k_JcBSj4HeDoKl6pLrC4o6-BoJVAghBTGngFJK9CRHI"; // ARCHIVO CONCENTRADO VERDADEROS PRUEBA

// const PROVISIONADAS_ID = "1BNEkeUhABA7hDKU06JSUbUm91R-8cMUQRfcaTHnRuFo"; //  ARCHIVO CONCENTRADO PROVISIONADAS PRUEBA

  
  

// const FOLDER_ID = "11h92HeG5egXPXJ2kinKHeuOkn9PTsZ5M"; // FOLDER PRUEBA 2025

// const CONCENTRADO_ID = "19ClyD5h-tP1oSFkWpbHUEvK4M9P04XGVCQayGfju15w"; // ARCHIVO PRUEBA CONCENTRADO VERDADEROS 2025

// const FOLDER_ID = "1c4LCLmvT2rd7pX1DGgB9zuZN47qQmWIj"; // FOLDER MARZO 2025

// const CONCENTRADO_ID = "15UjUCirVWB0ucqVhizvh_MyO7RzSOFROP0OCh8bsT50"; // ARCHIVO CONCENTRADO VERDADEROS MARZO 2025

// const PROVISIONADAS_ID = "13yuUhAB_nxJpp4aetLYcJ2ypduwIRR7PLSPPO9a3pGY"; //  ARCHIVO CONCENTRADO PROVISIONADAS MARZO 2025

// const FOLDER_ID = "1_3iaIrf4OxpFECiT6IM7h8x3riA_zV2B"; // FOLDER DIAS MAYO 2025

// const CONCENTRADO_ID = "1S3j7HbYxuvvege_bXSMI_Sd7oJgxCnCM_wjQqJiGxe4"; // ARCHIVO CONCENTRADO VERDADEROS MAYO 2025

// const FOLDER_ID = "1kJCD3pg3xsQcc0eoMndevKDzogIZKFW2"; // FOLDER DIAS JUNIO 2025

// const CONCENTRADO_ID = "18GWCYp5ovoq4sZOrpNIyi7WSWGHJr9TNg701gjohQ94"; // ARCHIVO CONCENTRADO VERDADEROS JUNIO 2025

// const FOLDER_ID = "1fqycJX8niIYi0F2ALBhK8SN_2LNwwC8I"; // FOLDER DIAS JULIO 2025

// const CONCENTRADO_ID = "1dQCvt2y-N1X29jUc-IKr4hjym2-1vRLMwqqzCR4js3Y"; // ARCHIVO CONCENTRADO VERDADEROS JULIO 2025

// const FOLDER_ID = "1TAwrG_NQDjgYzMdHfmQqRamFp--ZFP-x"; // FOLDER DIAS AGOSTO 2025

// const CONCENTRADO_ID = "1J9t5wzmrPqiKaDzvvlrXCuilPFzxrzURr0NGnlNXB4o"; // ARCHIVO CONCENTRADO VERDADEROS AGOSTO 2025

// const FOLDER_ID = "17ZZU6AZm0UlifU6YzBsXlriLJSYiA3tQ"; // FOLDER DIAS SEPTIEMBRE 2025

// const CONCENTRADO_ID = "1ptqEqVE9Y5iur7eYSDNl6sIKalC0j8-0cF9mSrfWlbQ"; // ARCHIVO CONCENTRADO VERDADEROS SEPTIEMBRE 2025

// const FOLDER_ID = "1FN9kn9i_clZTZBjDnF94MIjbr_WcYKrc"; // FOLDER DIAS OCTUBRE 2025

// const CONCENTRADO_ID = "1q42UgfwZeoFUM-LjwDDsU9RSN4KdXgJxU7_Y8YNOzxQ"; // ARCHIVO CONCENTRADO VERDADEROS OCTUBRE 2025

// const FOLDER_ID = "1RBA-oMGt3fgTWxjBoekgu9XJLPa9I1Qj"; // FOLDER DIAS NOVIEMBRE 2025

// const CONCENTRADO_ID = "12OQZxEATGy6ib1fJ6-HAmdWKvZx9edUGsxhqiT6rcvU"; // ARCHIVO CONCENTRADO VERDADEROS NOVIEMBRE 2025

// const FOLDER_ID = "1D2DMsz9ZRrflXN_kkk9TGvDHglzm_0LY"; // FOLDER DIAS DICIEMBRE 2025

// const CONCENTRADO_ID = "1WgPr_x1i7tuKN4bQToQ7dtVRT8TYi53qisVfwi3BsBI"; // ARCHIVO CONCENTRADO VERDADEROS DICIEMBRE 2025

// const FOLDER_ID = "1Zh45OaSyPEMbGBrZAetnd5AhliSeEKCB"; // FOLDER DIAS ENERO 2026 (Kabzo)

  

////////////////////////////// TRANSICION (KABZO -> GRUPO CISE)

  

// const FOLDER_ID = "1ftrEq_JbD-MkBEUVvCIxMRDB5nmKoKbD"; // FOLDER DIAS ENERO 2026 (Grupo CISE)

// const CONCENTRADO_ID = "1LqDN21e5ejy8ZNQofuBNpA_AGBHldsuM_W_cu8G4AEo"; // ARCHIVO CONCENTRADO VERDADEROS ENERO 2026

// const FOLDER_ID = "1as13CI_QOfxIeWz1VQ3tBllTq0GuyS4w"; // FOLDER DIAS FEBRERO 2026 (Grupo CISE)

// const CONCENTRADO_ID = "1nCOgf8qxjQ5PAX811au-kGoZlIsbcGO5wlsAkyZjw0Y"; // ARCHIVO CONCENTRADO VERDADEROS FEBRERO 2026

// const FOLDER_ID = "15vXNerkos0wLpEt_HunCxN_BGgdT3Xsq"; // FOLDER DIAS MARZO 2026 (Grupo CISE)

// const CONCENTRADO_ID = "1tMCBB-0hxfSrdecpt-PP3zzSb8i0aT2zhIgIg4qgtcA"; // ARCHIVO CONCENTRADO VERDADEROS MARZO 2026

// const FOLDER_ID = "1DaxFsv_BEXexYI3dc9Pb1Da-UylOEjC4"; // FOLDER DIAS ABRIL 2026 (Grupo CISE)

// const CONCENTRADO_ID = "1wM0MZiqbTp754tPX3_u12fEA33YSePDHkCKbkrchVF4"; // ARCHIVO CONCENTRADO VERDADEROS ABRIL 2026

// const FOLDER_ID = "1wFJo71a-F73b4HDXfd5JVGVGuetOOQLT"; // FOLDER DIAS MAYO 2026 (Grupo CISE)

// const CONCENTRADO_ID = "1FadH6Vd2EzMdytSe0daV5rSMdavCgVumvUgYEsBc4dg"; // ARCHIVO CONCENTRADO VERDADEROS MAYO 2026

// const FOLDER_ID = "1SxGgPPH1iePGuwUqCwd2OBfVtuduCiIv"; // FOLDER DIAS JUNIO 2026 (Grupo CISE)

// const CONCENTRADO_ID = "16svXuiD2L4R1I2nzpaNxnhCaTLWwE0a3wwaeDDSfXGM"; // ARCHIVO CONCENTRADO VERDADEROS JUNIO 2026

// const FOLDER_ID = "1cbWgn_DzveYtHC6lh6ECvRqpaHkdohYj"; // FOLDER DIAS JULIO 2026 (Grupo CISE)

// const CONCENTRADO_ID = "1fecpw0wki2J6BZUSbM5kRr06FMf4gnVewWBy9xvRIEM"; // ARCHIVO CONCENTRADO VERDADEROS JULIO 2026

const FOLDER_ID = "1uX4BZ8JNKsbOc9LNWI3qpneF4jZOeYKw"; // FOLDER DIAS AGOSTO 2026 (Grupo CISE)

const CONCENTRADO_ID = "12pWEJvtRmV-I3uzFy6gBorPlRxWKC8xfGm-soB9P0Ig"; // ARCHIVO CONCENTRADO VERDADEROS AGOSTO 2026

const PROVISIONADAS_ID = "1ECcbb4qJNGsffkB58U6ePKZOCEshc3b2aqhQBscpCKw"; //  ARCHIVO CONCENTRADO PROVISIONADAS

  

// VERSION 2 - INCLUIR HASTA COLUMNA AJ PARA LAS 3 COLUMNAS DE COBRANZA EN HOJA DOCFACT

// VERSION 3 - INCLUIR "FUERA DE ALCANCE" A MANDAR AL ARCHIVO PROVISIONADAS EN ESTATUS

//             CAMBIO DE COLUMNAS U → V, N → O

  

/**

 * Función genérica para actualizar checkboxes,

 * usuario de captura y fecha de ejecución.

 *

 * Lee un solo bloque de 6 columnas:

 * MOVS:                   P:U

 * DOCUMENTOS FACTURACION: W:AB

 */

function actualizarCaptura(nombreHoja, colInicio) {

  const ui = SpreadsheetApp.getUi();

  try {

    const hoja = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(nombreHoja);

    if (!hoja) {

      throw new Error(`No se encontró la hoja "${nombreHoja}".`);

    }

    const lastRow = hoja.getRange("A1")

      .getNextDataCell(SpreadsheetApp.Direction.DOWN)

      .getRow();

    if (lastRow < 2) {

      ui.alert("No existen registros para procesar.");

      return;

    }

    // Lee un solo bloque:

    // MOVS: P:U

    // DOCS: W:AB

    const datos = hoja.getRange(2, colInicio, lastRow - 1, 6).getValues();

    const checkboxes = [];

    const capturadoPorCodigo = [];

    const fechaEjecucion = [];

    const fechaActual = new Date();

    for (let i = 0; i < datos.length; i++) {

      const estado = String(datos[i][0]).trim().toUpperCase();

      const valor1 = datos[i][1];

      const valor2 = datos[i][2];

      const capturado = datos[i][4];

      const fecha = datos[i][5];

      const encontrado = (valor1 === 1) || (valor2 === 1);

      // Si está cancelada o pendiente nunca marcar checkbox

      let checkbox;

      if (estado === "CANCELADA" || estado === "PENDIENTE DE CONCILIAR") {

        checkbox = false;

      } else if (estado === "CONCILIADA") {

        checkbox = true;

      } else {

        checkbox = encontrado || capturado !== "";

      }

      checkboxes.push([checkbox]);

      if (encontrado && capturado === "") {

        capturadoPorCodigo.push(["Capturado por código"]);

        fechaEjecucion.push([fechaActual]);

      } else if (capturado !== "") {

        capturadoPorCodigo.push([capturado]);

        fechaEjecucion.push([fecha]);

      } else {

        capturadoPorCodigo.push([null]);

        fechaEjecucion.push([null]);

      }

    }

    // El checkbox siempre es la 4.ª columna del bloque

    hoja.getRange(2, colInicio + 3, checkboxes.length, 1)

      .setValues(checkboxes);

    // Capturado siempre es la 5.ª columna

    hoja.getRange(2, colInicio + 4, capturadoPorCodigo.length, 1)

      .setValues(capturadoPorCodigo);

    // Fecha siempre es la 6.ª columna

    hoja.getRange(2, colInicio + 5, fechaEjecucion.length, 1)

      .setValues(fechaEjecucion);

    if(nombreHoja === `DOCUMENTOS FACTURACION`) return

    // ui.alert(

    //   "Proceso finalizado",

    //   `Las hojas "DOCUMENTOS FACTURACION" y "MOVS" se procesaron correctamente.\n\nSe procesaron ${datos.length} registros.`,

    //   ui.ButtonSet.OK

    // );

    const html = HtmlService.createHtmlOutput(`

    <!DOCTYPE html>

    <html>

    <head>

      <style>

        body{

          font-family: Arial, sans-serif;

          text-align:center;

          padding:25px;

          color:#333;

        }

        h2{

          color:#188038;

          margin-bottom:10px;

        }

        p{

          font-size:15px;

          margin:8px 0;

        }

        .filas{

          margin:20px auto;

          width:180px;

          padding:12px;

          border-radius:10px;

          background:#E8F5E9;

          color:#188038;

          font-size:28px;

          font-weight:bold;

          border:1px solid #A5D6A7;

        }

        button{

          margin-top:20px;

          background:#1A73E8;

          color:white;

          border:none;

          border-radius:6px;

          padding:10px 24px;

          font-size:14px;

          cursor:pointer;

        }

        button:hover{

          background:#1669C1;

        }

      </style>

    </head>

    <body>

    <h2>✅ Proceso finalizado</h2>

    <p>Las hojas</p>

    <b>DOCUMENTOS FACTURACION</b><br>

    <b>MOVS</b>

    <p>se procesaron correctamente.</p>

    <p>Filas Procesadas</p>

    <div class="filas">

    ${datos.length}

    </div>

    <button onclick="google.script.host.close()">

    Aceptar

    </button>

    </body>

    </html>

    `)

    .setWidth(360)

    .setHeight(400);

    SpreadsheetApp.getUi().showModalDialog(html, "Proceso finalizado");

  } catch (error) {

    Logger.log(error);

    console.error(error);

    ui.alert(

      "Error al ejecutar la función",

      `${error.message}\n\n${error.stack}`,

      ui.ButtonSet.OK

    );

  }

}

/**

 * DOCUMENTOS FACTURACION

 * Lee W:AB

 */

function upDocFact2() {

  actualizarCaptura(

    "DOCUMENTOS FACTURACION",

    23 // W

  );

}

/**

 * MOVS

 * Lee P:U

 */

function upMovs2() {

  actualizarCaptura(

    "MOVS",

    16 // P

  );

}

  

function info() {

  const ui = SpreadsheetApp.getUi();

  try {

    const ssOrigen = SpreadsheetApp.getActiveSpreadsheet();

    const sheetODF = ssOrigen.getSheetByName("DOCUMENTOS FACTURACION");

    const sheetOM = ssOrigen.getSheetByName("MOVS");

    if (!sheetODF) {

      throw new Error('No se encontró la hoja "DOCUMENTOS FACTURACION".');

    }

    if (!sheetOM) {

      throw new Error('No se encontró la hoja "MOVS".');

    }

    const lastRowDocs = sheetODF

      .getRange("A1")

      .getNextDataCell(SpreadsheetApp.Direction.DOWN)

      .getRow();

    const lastRowMovs = sheetOM

      .getRange("A1")

      .getNextDataCell(SpreadsheetApp.Direction.DOWN)

      .getRow();

    const rangoDatosODF = sheetODF

      .getRange(`A1:AE${lastRowDocs}`) // CAMBIAR A A1:AJ

      .getValues();

    const rangoDatosOM = sheetOM

      .getRange(`A1:V${lastRowMovs}`)

      .getValues();

    if (!rangoDatosODF.length || !rangoDatosOM.length) {

      ui.alert("No hay información para enviar.");

      return;

    }

    const tempVDF = [rangoDatosODF[0]];

    const tempVM = [rangoDatosOM[0]];

    const tempFDF = [rangoDatosODF[0]];

    const tempFM = [rangoDatosOM[0]];

    for (let i = 1; i < rangoDatosODF.length; i++) {

      const fila = rangoDatosODF[i];

      if (!fila[0]) {

        continue;

      }

      if (

        fila[21] === "PROVISIONADA" ||

        fila[21] === "DOLARES" ||

        fila[22] === "CANCELADA"

      ) {

        continue;

      }

      if (fila[25] === true) {  //  COLUMNA Z

        tempVDF.push(fila);

      } else {

        tempFDF.push(fila);

      }

    }

    for (let i = 1; i < rangoDatosOM.length; i++) {

      const fila = rangoDatosOM[i];

      if (!fila[0]) {

        continue;

      }

      if (

        fila[14] === "PROVISIONADA" ||

        fila[14] === "DOLARES" ||

        fila[15] === "CANCELADA"

      ) {

        continue;

      }

      if (fila[18] === true) {  //  COLUMNA S

        tempVM.push(fila);

      } else {

        tempFM.push(fila);

      }

    }

    const ssV = SpreadsheetApp.openById(CONCENTRADO_ID);

    const ssVDF = ssV.getSheetByName("DOCUMENTOS FACTURACION");

    const ssVM = ssV.getSheetByName("MOVS");

    if (!ssVDF || !ssVM) {

      throw new Error("No se encontraron las hojas en el archivo Concentrado.");

    }

    const ssF = ssOrigen.copy(`${ssOrigen.getName()} - FALSOS`);

    const ssFDF = ssF.getSheetByName("DOCUMENTOS FACTURACION");

    const ssFM = ssF.getSheetByName("MOVS");

    if (!ssFDF || !ssFM) {

      throw new Error("No se pudieron crear las hojas del archivo FALSOS.");

    }

    tempVDF.shift();

    tempVM.shift();

    const ssVDFLastRow =

      ssVDF

        .getRange("A:A")

        .getDisplayValues()

        .filter(fila => fila[0] !== "")

        .length + 1;

    const ssVMLastRow =

      ssVM

        .getRange("A:A")

        .getDisplayValues()

        .filter(fila => fila[0] !== "")

        .length + 1;

    if (tempVDF.length) {

      ssVDF

        .getRange(

          ssVDFLastRow,

          1,

          tempVDF.length,

          tempVDF[0].length

        )

        .setValues(tempVDF);

    }

    if (tempVM.length) {

      ssVM

        .getRange(

          ssVMLastRow,

          1,

          tempVM.length,

          tempVM[0].length

        )

        .setValues(tempVM);

    }

    if (tempFDF.length) {

      ssFDF.getRange(`A2:AB`).clear();

      ssFDF

        .getRange(

          1,

          1,

          tempFDF.length,

          tempFDF[0].length

        )

        .setValues(tempFDF);

    }

    if (tempFM.length) {

      ssFM.clearContents();

      ssFM

        .getRange(

          1,

          1,

          tempFM.length,

          tempFM[0].length

        )

        .setValues(tempFM);

    }

    const formulasRS = [];

    const formulasXY = [];

    const formulasACAD = [];

    for (let a = 0; a < tempFDF.length - 1; a++) {

      const fila = a + 2;

      formulasRS.push([

        `=IFERROR(

          VLOOKUP(

            $L${fila},

            INFO!A:B,2,0

          ),

        "-")`,

        `=IFERROR(

          VLOOKUP(

            $R${fila},

            INFO!B:C,2,0

          ),

        "-")`

      ]);

      formulasXY.push([

        `=COUNTIFS(

          MOVS!$I:$I,$B${fila},

          MOVS!$E:$E,$R${fila},

          MOVS!$B:$B,$S${fila},

          MOVS!$L:$L,FILTER(

            MOVS!$L:$L,

            MOVS!$I:$I = $B${fila},

            MOVS!$E:$E = $R${fila},

            MOVS!$B:$B = $S${fila},

            MOVS!$L:$L >= $F${fila}-0.99,

            MOVS!$L:$L <= $F${fila}+0.99

          )

        )`,

        `=COUNTIFS(

          MOVS!$I:$I,$B${fila},

          MOVS!$E:$E,$R${fila},

          MOVS!$B:$B,$S${fila},

          MOVS!$L:$L,FILTER(

            MOVS!$L:$L,

            MOVS!$I:$I = $B${fila},

            MOVS!$E:$E = $R${fila},

            MOVS!$B:$B = $S${fila},

            MOVS!$L:$L >= $G${fila}-0.99,

            MOVS!$L:$L <= $G${fila}+0.99

          )

        )`

      ]);

      formulasACAD.push([

        `=IF($W${fila}="CANCELADA","CANCELADA",IF($AH${fila}="MAS DE 90","COBRANZA VENCIDA",""))`,

        `=IF($I${fila}="Pendiente","PARCIALIDAD",IF($I${fila}="Pagada","CONCILIADA",""))`

      ]);

    }

    if (formulasRS.length) {

      ssFDF.getRange(2,18,formulasRS.length,2).setValues(formulasRS);

    }

    if (formulasXY.length) {

      ssFDF.getRange(2,24,formulasXY.length,2).setValues(formulasXY);

    }

    if (formulasACAD.length) {

      ssFDF.getRange(2,29,formulasACAD.length,2).setValues(formulasACAD);

    }

    ssFDF.getRange(`AF3:AH`).clearContent();

    // ssFDF.getRange(2, 32, 1, 3).setValues(

    //   [[`=ARRAYFORMULA(

    //       SI(

    //       P2:P="",

    //       "",

    //       SI(

    //       ESNUMERO(P2:P),

    //       ENTERO(P2:P),

    //       FECHA(

    //       VALOR(REGEXEXTRACT(P2:P,"(\d{4})")),

    //       VALOR(REGEXEXTRACT(P2:P,"^\d{1,2}/(\d{1,2})")),

    //       VALOR(REGEXEXTRACT(P2:P,"^(\d{1,2})"))

    //       )

    //       )

    //       )

    //       )`

    //     ,

    //   `=ARRAYFORMULA(

    //     SI(

    //       AF2:AF="",

    //       "",

    //       AF2:AF-O2:O

    //     )

    //   )`

    //   ,

    //   `=ARRAYFORMULA(

    //     IF(

    //       AF2:AF="",

    //       "",

    //       IF(

    //         AF2:AF<30,

    //         "MENOS DE 30",

    //         IF(

    //           AF2:AF<60,

    //           "MENOS DE 60",

    //           IF(

    //             AF2:AF<90,

    //             "MENOS DE 90",

    //             "MAS DE 90"

    //           )

    //         )

    //       )

    //     )

    //   )`]]

    // );

    const formulasQR = [];

    for (let a = 0; a < tempFM.length - 1; a++) {

      const fila = a + 2;

      formulasQR.push([

        `=COUNTIFS(

          'DOCUMENTOS FACTURACION'!$B:$B,$I${fila},

          'DOCUMENTOS FACTURACION'!$R:$R,$E${fila},

          'DOCUMENTOS FACTURACION'!$S:$S,$B${fila},

          'DOCUMENTOS FACTURACION'!$F:$F,FILTER(

            'DOCUMENTOS FACTURACION'!$F:$F,

            'DOCUMENTOS FACTURACION'!$B:$B = $I${fila},

            'DOCUMENTOS FACTURACION'!$R:$R = $E${fila},

            'DOCUMENTOS FACTURACION'!$S:$S = $B${fila},

            'DOCUMENTOS FACTURACION'!$F:$F >= $L${fila}-0.99,

            'DOCUMENTOS FACTURACION'!$F:$F <= $L${fila}+0.99

          )

        )`,

        `=COUNTIFS(

          'DOCUMENTOS FACTURACION'!$B:$B,$I${fila},

          'DOCUMENTOS FACTURACION'!$R:$R,$E${fila},

          'DOCUMENTOS FACTURACION'!$S:$S,$B${fila},

          'DOCUMENTOS FACTURACION'!$G:$G,FILTER(

            'DOCUMENTOS FACTURACION'!$G:$G,

            'DOCUMENTOS FACTURACION'!$B:$B = $I${fila},

            'DOCUMENTOS FACTURACION'!$R:$R = $E${fila},

            'DOCUMENTOS FACTURACION'!$S:$S = $B${fila},

            'DOCUMENTOS FACTURACION'!$G:$G >= $L${fila}-0.99,

            'DOCUMENTOS FACTURACION'!$G:$G <= $L${fila}+0.99

          )

        )`

      ]);

    }

    if (formulasQR.length) {

      ssFM.getRange(2,17,formulasQR.length,2).setValues(formulasQR);

    }

    const ssFFile = DriveApp.getFileById(ssF.getId());

    ssFFile.moveTo(

      DriveApp.getFolderById(FOLDER_ID)

    );

  

    const filasPPD = actualizarPPD(tempVDF, ssV);

  

    const enviados =

      tempVDF.length +

      tempVM.length;

    const falsos =

      tempFDF.length - 1 +

      tempFM.length - 1;

//     ui.alert(

//       "Proceso finalizado",

//       `La información se envió correctamente.

// Registros enviados al concentrado: ${enviados}

// Registros enviados al archivo FALSOS: ${falsos}`,

//       ui.ButtonSet.OK

//     );

    const html = HtmlService.createHtmlOutput(`

    <!DOCTYPE html>

    <html>

    <head>

      <style>

        body{

          font-family: Arial, sans-serif;

          text-align:center;

          padding:25px;

          color:#333;

        }

        h2{

          color:#188038;

          margin-bottom:10px;

        }

        p{

          font-size:15px;

          margin:8px 0;

        }

        .filas{

          margin:20px auto;

          width:180px;

          padding:12px;

          border-radius:10px;

          background:#E8F5E9;

          color:#188038;

          font-size:28px;

          font-weight:bold;

          border:1px solid #A5D6A7;

        }

        button{

          margin-top:20px;

          background:#1A73E8;

          color:white;

          border:none;

          border-radius:6px;

          padding:10px 24px;

          font-size:14px;

          cursor:pointer;

        }

        button:hover{

          background:#1669C1;

        }

      </style>

    </head>

    <body>

    <h2>✅ Proceso finalizado</h2>

    <p>Se enviaron correctamente las filas al archivo del Concentrado.</p>

    <b>Filas enviadas al Concentrado:</b>

    <div  class="filas">${enviados}</div><br><br>

    <b>Filas enviadas al archivo FALSOS</b>

    <div  class="filas">${falsos}</div><br><br>

    <b>Filas actualizadas en PPD</b>

    <div  class="filas">${filasPPD}</div><br><br>

    <button onclick="google.script.host.close()">

    Aceptar

    </button>

    </body>

    </html>

    `)

    .setWidth(360)

    .setHeight(550);

    SpreadsheetApp.getUi().showModalDialog(html, "Proceso finalizado");

  } catch (error) {

    Logger.log(error);

    console.error(error);

    ui.alert(

      "Error al ejecutar enviarInfo()",

      `${error.message}

${error.stack}`,

      ui.ButtonSet.OK

    );

  }

}

  

function prov() {

  const ui = SpreadsheetApp.getUi();

  try {

    const ssOrigen = SpreadsheetApp.getActiveSpreadsheet();

    const sheetODF = ssOrigen.getSheetByName("DOCUMENTOS FACTURACION");

    const sheetOM = ssOrigen.getSheetByName("MOVS");

    if (!sheetODF) {

      throw new Error('No existe la hoja "DOCUMENTOS FACTURACION".');

    }

    if (!sheetOM) {

      throw new Error('No existe la hoja "MOVS".');

    }

    const lastRowDocs = sheetODF

      .getRange("A1")

      .getNextDataCell(SpreadsheetApp.Direction.DOWN)

      .getRow();

    const lastRowMovs = sheetOM

      .getRange("A1")

      .getNextDataCell(SpreadsheetApp.Direction.DOWN)

      .getRow();

    const rangoDatosODF =

      sheetODF

      .getRange(`A1:AE${lastRowDocs}`)

      .getDisplayValues();

    const rangoDatosOM =

      sheetOM

      .getRange(`A1:V${lastRowMovs}`)

      .getDisplayValues();

    if (!rangoDatosODF.length || !rangoDatosOM.length) {

      ui.alert("No hay información para enviar.");

      return;

    }

    const tempPDF = [rangoDatosODF[0]];

    const tempPM = [rangoDatosOM[0]];

    for (let i = 1; i < rangoDatosODF.length; i++) {

      const fila = rangoDatosODF[i];

      if (!fila[0]) continue;

      if (

        (fila[21] === "PROVISIONADA" || fila[21] === "DOLARES" || fila[22] === "CANCELADA")

      ) {

        tempPDF.push(fila);

      }

    }

    for (let i = 1; i < rangoDatosOM.length; i++) {

      const fila = rangoDatosOM[i];

      if (!fila[0]) continue;

      if (

        (fila[14] === "PROVISIONADA" || fila[14] === "DOLARES" || fila[15] === "CANCELADA")

      ) {

        tempPM.push(fila);

      }

    }

    const ssP =

      SpreadsheetApp.openById(PROVISIONADAS_ID);

    const ssPDF =

      ssP.getSheetByName("DOCUMENTOS FACTURACION");

    const ssPM =

      ssP.getSheetByName("MOVS");

    if (!ssPDF || !ssPM) {

      throw new Error("No se encontraron las hojas del archivo de provisionadas.");

    }

    tempPDF.shift();

    tempPM.shift();

    if (tempPDF.length) {

      ssPDF

        .getRange(

          ssPDF.getLastRow() + 1,

          1,

          tempPDF.length,

          tempPDF[0].length

        )

        .setValues(tempPDF);

    }

    if (tempPM.length) {

      ssPM

        .getRange(

          ssPM.getLastRow() + 1,

          1,

          tempPM.length,

          tempPM[0].length

        )

        .setValues(tempPM);

    }

//     ui.alert(

//       "Proceso finalizado",

//       `Se enviaron correctamente los registros al archivo de Provisionadas.

// DOCUMENTOS FACTURACION: ${tempPDF.length}

// MOVS: ${tempPM.length}`,

//       ui.ButtonSet.OK

//     );

    const html = HtmlService.createHtmlOutput(`

    <!DOCTYPE html>

    <html>

    <head>

      <style>

        body{

          font-family: Arial, sans-serif;

          text-align:center;

          padding:25px;

          color:#333;

        }

        h2{

          color:#188038;

          margin-bottom:10px;

        }

        p{

          font-size:15px;

          margin:8px 0;

        }

        .filas{

          margin:20px auto;

          width:180px;

          padding:12px;

          border-radius:10px;

          background:#E8F5E9;

          color:#188038;

          font-size:28px;

          font-weight:bold;

          border:1px solid #A5D6A7;

        }

        button{

          margin-top:20px;

          background:#1A73E8;

          color:white;

          border:none;

          border-radius:6px;

          padding:10px 24px;

          font-size:14px;

          cursor:pointer;

        }

        button:hover{

          background:#1669C1;

        }

      </style>

    </head>

    <body>

    <h2>✅ Proceso finalizado</h2>

    <p>Se enviaron correctamente las filas al archivo de Provisionadas.</p>

    <b>DOCUMENTOS FACTURACION:</b>

    <div  class="filas">${tempPDF.length}</div><br><br>

    <b>MOVS:</b>

    <div  class="filas">${tempPM.length}</div><br><br>

    </div>

    <button onclick="google.script.host.close()">

    Aceptar

    </button>

    </body>

    </html>

    `)

    .setWidth(360)

    .setHeight(550);

    SpreadsheetApp.getUi().showModalDialog(html, "Proceso finalizado");

  }

  catch (error) {

    Logger.log(error);

    console.error(error);

    ui.alert(

      "Error al ejecutar enviarProv()",

      `${error.message}

${error.stack}`,

      ui.ButtonSet.OK

    );

  }

}

  

function actualizarPPD(tempVDF, ssV){

  

  let registrosActualizados = 0;

  

  const hojaPPD = ssV.getSheetByName("PPD");

  

  if (!hojaPPD) {

    throw new Error('No se encontró la hoja "PPD".');

  }

  

  const lastRow = hojaPPD.getLastRow();

  

  if (lastRow < 5 || tempVDF.length === 0) return;

  

  // A:AK

  const datosPPD = hojaPPD.getRange(5, 1, lastRow - 4, 37).getValues();

  

  // ===========================

  // Mantener solamente el registro

  // más reciente por B + C

  // ===========================

  

  const movimientos = new Map();

  

  tempVDF.forEach(fila => {

    const llave = `${fila[1]}|${fila[2]}`;

    // Siempre sobrescribe.

    // Como tempVDF está ordenado por fecha ascendente,

    // al finalizar quedará la fila más reciente.

    movimientos.set(llave, fila);

  });

  

  // ===========================

  // Arreglos para actualizar

  // ===========================

  

  const nuevosMontos = [];

  const nuevosEstatus = [];

  const nuevoEstadoPago = [];

  

  for (let i = 0; i < datosPPD.length; i++) {

  

    const filaPPD = datosPPD[i];

  

    const llave = `${filaPPD[1]}|${filaPPD[2]}`;

  

    if (movimientos.has(llave)) {

  

      const movimiento = movimientos.get(llave);

  

      const montoOriginal = movimiento[7];

  

      const montoAnterior = Number(filaPPD[7]);

      const montoNuevo = Number(montoOriginal);

  

      // Columna W (índice 22)

      const estatusActual = String(filaPPD[22]).trim().toUpperCase();

  

      // Si está CANCELADA, conservar el monto actual

      const montoFinal =

        estatusActual === "CANCELADA"

          ? filaPPD[7]

          : montoOriginal;

  

      nuevosMontos.push([montoFinal]);

  

      const esPagada =

        montoOriginal === null ||

        montoOriginal === "" ||

        montoOriginal === "$-" ||

        montoNuevo === 0;

  

      nuevosEstatus.push([

        esPagada

          ? "CONCILIACION"

          : filaPPD[36]

      ]);

      nuevoEstadoPago.push([

        esPagada

          ? "Pagada"

          : filaPPD[8]

      ]);

  

      // Solo contar si realmente cambió el monto

      if (

        estatusActual !== "CANCELADA" &&

        montoAnterior !== montoNuevo

      ) {

        registrosActualizados++;

      }

  

    } else {

  

      nuevosMontos.push([filaPPD[7]]);

      nuevoEstadoPago.push([filaPPD[8]]);

      nuevosEstatus.push([filaPPD[36]]);

  

    }

  

  }

  

  // H

  hojaPPD

    .getRange(5, 8, nuevosMontos.length, 1)

    .setValues(nuevosMontos);

  

  // I

  hojaPPD

    .getRange(5, 9, nuevoEstadoPago.length, 1)

    .setValues(nuevoEstadoPago);

  

  // AK

  hojaPPD

    .getRange(5, 37, nuevosEstatus.length, 1)

    .setValues(nuevosEstatus);

  

  return registrosActualizados;

  

}

## Relacionados

- [[Concentrado Conciliacion Oper-Fact/Cuadre Diario/Código.gs]] — define el menú "Transferir Datos" que dispara estas funciones
- [[Contexto General]] — contexto del proceso de conciliación Diario → Concentrado
- [[Proyecto PPD]] — define `actualizarPPD(tempVDF, ssV)`, la función que describe esa nota
- [[Actualización PPD]] — implementa la comparación tempVDF vs PPD (columnas H/AK, CANCELADA, CONCILIACION) descrita ahí
